import type { pathModel } from "./models/path-model";

export const paths: pathModel = {
  home: "/",
  settings: {
    history: "/settings/history",
    thresholds: "/settings/thresholds",
  },
  supports: {
    donation: "/supports/donation",
    gitStar: "/supports/git-start",
  },
};
