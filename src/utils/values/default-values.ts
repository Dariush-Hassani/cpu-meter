import type { DefaultValuesModel } from "../../models/default-values-model";
import { ThemeModeEnum } from "../../models/theme-model";

export const defaultValues: DefaultValuesModel = {
  themeMode: ThemeModeEnum.DARK,
};
